{
  "type": "object",
  "properties": {
    "paths": {
      "type": "array",
      "title": "Data values that should be monitored",
      "items": { "type": "string" }
    },
    "startton": {
      "description": "Key which must true before the plugin will start",
      "type": "string"
    },
    "outputs": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "switchpath": { "type": "string" },
          "triggerstates": {
            "type": "array",
            "items": { "type": "string", "enum": [ "warn", "alert", "alarm", "emergency" ] }
          }
        }
      }
    }
  }
}
